{
  "framework": "vite",
  "installCommand": "pnpm install --config.onlyBuiltDependencies=esbuild",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
